# Uncomment these two lines of configuration to
# enable persistent storage in db/dyproso.
################################################
spring.datasource.url=jdbc:mysql://dyprosodb.ct1lrouepcxg.eu-central-1.rds.amazonaws.com:3306/dyprosodb
spring.datasource.username = root
spring.datasource.password = ********
spring.jpa.show-sql = true
spring.jpa.hibernate.ddl-auto= update
#multipleFiles
spring.servlet.multipart.enabled=true

# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB

# Max file size.
spring.servlet.multipart.max-file-size=10MB

# Max Request Size
spring.servlet.multipart.max-request-size=15MB


spring.mail.host=msx.tu-dresden.de
spring.mail.port=587
spring.mail.username=dyproso
spring.mail.password=*******
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true


server.tomcat.max-swallow-size=-1

server.port = 5000